{
  "scripts": {
    "pretty": "prettier --write **/*.{js, ts}"
  },
  "husky": {
    "hooks": {
      "pre-commit": "./node_modules/.bin/lint-staged --config .lintstagedrc"
    }
  },
  "dependencies": {},
  "devDependencies": {
    "husky": "^0.14.3",
    "lint-staged": "^7.0.0",
    "prettier": "^1.11.1"
  }
}
