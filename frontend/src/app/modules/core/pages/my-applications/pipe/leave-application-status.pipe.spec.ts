import { LeaveApplicationStatusPipe } from './leave-application-status.pipe';

describe('LeaveApplicationStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new LeaveApplicationStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
