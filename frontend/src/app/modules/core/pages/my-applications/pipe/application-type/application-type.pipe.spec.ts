import { ApplicationTypePipe } from './application-type.pipe';

describe('ApplicationTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ApplicationTypePipe();
    expect(pipe).toBeTruthy();
  });
});
